# LDA 参数优化对比报告

## 说明

本报告记录 LDA 模型的参数优化过程。为了和 EEGNet 对齐，时间窗口统一保留 5 个代表性配置：`0-600`、`0-500`、`0-400`、`100-500`、`150-450`。原先的 `0-480` 不再作为单独实验项，统一替换为 `0-500`；其他窗口对比实验保留。

## 优化目标

优化目标不是只追求单个事件分类指标，而是综合比较三类结果：

- 字符级交叉验证正确数：主要指标，反映训练字符上的稳定识别能力。
- 事件级 Balanced Accuracy 和 AUC：辅助指标，反映单次闪烁事件的排序与区分能力。
- Unknown 字符正确数：最终参考指标，只用于验证泛化效果，不参与模型训练。

## 共同对齐参数

LDA 与 EEGNet 对齐的共同实验参数如下：

| 参数 | 取值 | 说明 |
| --- | --- | --- |
| 时间窗口 | `0-600`、`0-500`、`0-400`、`100-500`、`150-450` | 两个模型使用同一组窗口做对比 |
| 通道方案 | 全 20 通道、LDA Top-12 通道 | 全通道作为基线，Top-12 作为降维方案 |
| 主要评价 | CV 字符正确数 | 优先看字符级结果 |
| 辅助评价 | 事件 BalAcc、事件 AUC、Unknown 正确数 | 用于解释模型稳定性和泛化表现 |

## 模型独有参数

LDA 的独有参数主要是特征构造和判别器正则化参数：

| 参数 | 含义 |
| --- | --- |
| `bins` | 将时间窗口划分成若干时间段，对每个通道提取分段均值特征 |
| `shrinkage` | LDA 协方差收缩强度，用于缓解特征维度较高时的估计不稳定 |
| 特征维度 | `通道数 × bins`，例如全 20 通道、12 bins 时为 240 维 |

## 优化过程与结果

第一步是时间窗口优化。固定全 20 通道后，对不同窗口分别搜索合适的 `bins` 和 `shrinkage`。`0-600 ms` 的 CV 字符正确数最高，为 `11/12`，事件 BalAcc 为 `0.732`，事件 AUC 为 `0.800`，Unknown 为 `5/8`。这个窗口训练字符表现最好，但 Unknown 不如部分较短窗口，说明过长窗口可能引入晚期噪声。

`0-500 ms` 的 CV 字符正确数为 `10/12`，事件 BalAcc 为 `0.748`，事件 AUC 为 `0.801`，Unknown 为 `6/8`。虽然字符正确数低于 `0-600 ms`，但事件级指标略高，Unknown 也更好，并且它与 EEGNet 的主要输入窗口一致，因此作为后续统一对比窗口。`0-400 ms` 同样达到 `10/12` 和 Unknown `6/8`，但 AUC 为 `0.784`，低于 `0-500 ms`。`100-500 ms` 和 `150-450 ms` 的 CV 字符正确数均为 `8/12`，说明去掉早期响应后训练字符识别下降。

第二步是通道策略优化。固定 `0-500 ms + 12 bins + shrinkage=0.03` 后，对比全 20 通道、Top-K 通道和通道加权。全 20 通道基线为 `10/12`，事件 BalAcc `0.748`，事件 AUC `0.801`，Unknown `6/8`。LDA Top-12 通道仍保持 `10/12` 和 Unknown `6/8`，但事件 BalAcc 提升到 `0.759`，事件 AUC 提升到 `0.816`，说明去掉低判别力通道后，事件排序更清晰。

通道加权 `alpha=1.5` 的 Unknown 可以达到 `7/8`，但 CV 字符正确数降为 `9/12`；Top-10 也能达到 Unknown `7/8`，但 CV 字符正确数只有 `8/12`。因此这些方案可作为参考，但不作为主推荐配置。

## 窗口参数对比

| 窗口 | bins | shrinkage | 特征维度 | CV字符正确数 | CV字符准确率 | 事件BalAcc | 事件AUC | Unknown正确 | Unknown预测 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 0-600 | 10 | 0.08 | 200 | 11 | 0.917 | 0.732 | 0.800 | 5/8 | 2BL5CXKM |
| 0-500 | 12 | 0.03 | 240 | 10 | 0.833 | 0.748 | 0.801 | 6/8 | 2BF5UXKM |
| 0-400 | 6 | 0.03 | 120 | 10 | 0.833 | 0.720 | 0.784 | 6/8 | 2TL5UXKM |
| 100-500 | 10 | 0.03 | 200 | 8 | 0.667 | 0.734 | 0.773 | 6/8 | 2TF5UXEM |
| 150-450 | 6 | 0.03 | 120 | 8 | 0.667 | 0.708 | 0.762 | 7/8 | 2TF5IXGM |

## 通道参数对比

| 通道策略 | TopK | 特征维度 | CV字符正确数 | CV字符准确率 | 事件BalAcc | 事件AUC | Unknown正确 | Unknown预测 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Top-12 channels | 12 | 144 | 10 | 0.833 | 0.759 | 0.816 | 6/8 | 2TF5OXGM |
| Full channels baseline |  | 240 | 10 | 0.833 | 0.748 | 0.801 | 6/8 | 2BF5UXKM |
| Weighted channels alpha=1.5 |  | 240 | 9 | 0.750 | 0.746 | 0.808 | 7/8 | 2TF5UXKM |
| Weighted channels alpha=1 |  | 240 | 9 | 0.750 | 0.744 | 0.807 | 7/8 | 2TF5UXKM |
| Top-16 channels | 16 | 192 | 8 | 0.667 | 0.738 | 0.807 | 6/8 | 2BF5OXKM |
| Top-10 channels | 10 | 120 | 8 | 0.667 | 0.748 | 0.804 | 7/8 | 2TF51XKM |
| Top-8 channels | 8 | 96 | 7 | 0.583 | 0.719 | 0.787 | 4/8 | 8NF51XGM |
| Top-6 channels | 6 | 72 | 7 | 0.583 | 0.698 | 0.766 | 4/8 | 8NF51XGM |
| Top-4 channels | 4 | 48 | 7 | 0.583 | 0.705 | 0.762 | 5/8 | 8NF51XKM |

## 推荐配置

LDA 推荐保留两个配置：

- 稳健基线：`0-500 ms + 全 20 通道 + 12 bins + shrinkage=0.03`，结果为 CV `10/12`、事件 AUC `0.801`、Unknown `6/8`。
- 降维优化：`0-500 ms + LDA Top-12 通道 + 12 bins + shrinkage=0.03`，结果为 CV `10/12`、事件 AUC `0.816`、Unknown `6/8`。

## 结论

LDA 的窗口实验说明，`0-600 ms` 在训练字符上最高，但 `0-500 ms` 在事件级指标和 Unknown 泛化上更均衡，也更便于和 EEGNet 对齐。通道实验说明，Top-12 在不损失字符正确数的情况下提升了事件 BalAcc 和 AUC，因此可以作为 LDA 的主要优化配置。
